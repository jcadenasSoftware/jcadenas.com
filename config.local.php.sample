<?php
// Copy this file to config.local.php on the server and fill real secrets
// This file should NOT be committed to version control.

// Strong 32-byte secret for encryption (change it!)
if (!defined('APP_SECRET')) {
    define('APP_SECRET', 'CHANGE_ME_TO_A_RANDOM_32_BYTE_SECRET');
}

// Secure downloads base directory (outside public_html). Adjust to your hosting path.
if (!defined('SECURE_DOWNLOAD_BASE')) {
    define('SECURE_DOWNLOAD_BASE', '/home/USER/secure_downloads/jcadenas/projects');
}

// SMTP credentials (Hostinger)
if (!defined('SMTP_HOST')) { define('SMTP_HOST', 'smtp.hostinger.com'); }
if (!defined('SMTP_PORT')) { define('SMTP_PORT', 465); }
if (!defined('SMTP_SECURE')) { define('SMTP_SECURE', 'ssl'); }
if (!defined('SMTP_USER')) { define('SMTP_USER', 'servicios@jcadenas.com'); }
if (!defined('SMTP_PASS')) { define('SMTP_PASS', 'YOUR_SMTP_PASSWORD'); }
